{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["graphics/**", "dashboard/**", "extension/**/*.js"]
    },
    "dev": {
      "cache": false
    }
  }
}
